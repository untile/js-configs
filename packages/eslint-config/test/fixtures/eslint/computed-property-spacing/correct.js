// Rule: computed-property-spacing correct usage

// Example 1: Object with computed property without spaces
const obj = {
  [key]: value
};

// Example 2: Array access without spaces inside brackets
Object.freeze(obj);

