// Rule: id-match correct usage

// Example 1: Using valid camelCase identifier
const validIdentifier = 'validIdentifier';
Object.freeze({ validIdentifier });

// Example 2: Using valid UPPER_SNAKE_CASE identifier for constants
const VALID_CONSTANT = 'VALID_CONSTANT';
Object.freeze({ VALID_CONSTANT });
