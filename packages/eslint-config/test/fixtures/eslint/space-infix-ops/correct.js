// Rule: space-infix-ops correct usage

// Example 1: Proper spacing around infix operators
const sum = a + b;
const product = num1 * num2;
const isGreater = value > 10;

noop(sum, product, isGreater);
