// Rule: prefer-template correct usage

// Example 1: Using template literals instead of string concatenation
const person = 'John';
const greeting = `Hello, ${person}! Welcome to our site.`;

noop(greeting);
