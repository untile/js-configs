// Rule: prefer-template incorrect usage

// Example 1: Using string concatenation instead of template literals
const person = 'John';
const greeting = 'Hello, ' + person + '! Welcome to our site.';

noop(greeting);
