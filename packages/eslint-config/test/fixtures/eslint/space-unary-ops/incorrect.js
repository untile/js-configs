// Rule: space-unary-ops incorrect usage

// Example 1: Missing spaces after unary word operators and incorrect spacing with unary operators
let counter = 0;
++ counter;
const value = - 5;

noop(counter, value);
