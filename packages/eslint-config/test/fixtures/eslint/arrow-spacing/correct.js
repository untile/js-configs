// Rule: arrow-spacing correct usage

// Example 1: No parameter
const noop = () => null;

// Example 2: Single parameter with arrow spacing
const ident = num => num;

// Example 3: Multiple parameters
const add = (num1, num2) => num1 + num2;

noop();
ident(1);
add(1, 2);
