/* eslint-disable no-unused-expressions */
// Rule: no-unexpected-multiline correct usage

// Example 1: Using semicolons to clarify function calls
const func = () => { noop(); };
`hello`;

noop(func);

