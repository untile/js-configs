// Rule: semi-spacing correct usage

// Example 1: Using semicolons to properly terminate statements
const greeting = 'Hello world';
const numbers = [1, 2, 3];
numbers.forEach(num => {
  noop(num);
});

noop(greeting, numbers);
