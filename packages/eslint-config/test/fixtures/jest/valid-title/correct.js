// Rule: jest/valid-title correct usage

// Example 1: Using valid test title with proper characters
describe('jest/valid-title', () => {
  test('should validate the input correctly', () => {
    expect(true).toBe(true);
  });
});
