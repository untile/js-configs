// Rule: jest/valid-expect correct usage

// Example 1: Using expect with proper matcher
describe('jest/valid-expect', () => {
  test('should use expect correctly', () => {
    expect(true).toBe(true);
  });
});
